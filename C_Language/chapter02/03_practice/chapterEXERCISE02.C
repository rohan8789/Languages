 #include<stdio.h>

 int main(int argc, char const *argv[])
 {
    // int i = -4, j, num;
    // j = (num < 0 ? 0 : num*num);
    // printf("%d\n", j); //garbage value.


    // int k, num = 30;
    // k = (num > 5 ?(num <= 10 ? 100 : 200) : 500);
    // printf("%d\n", k);   //200

    // int  j = 4;
    // (!j != 1 ? printf("hello\n") : printf("welcome\n"));

    // int tag = 0, code = 0;
    // if (tag == 0)
    // {
    //     (code > 1 ? printf("hello\n") : printf("hi\n"));
    // }
    // else
    // {
    //     printf("hi,hello!\n");
    // }


    // int i = 10, j;
    // i >= 5 ? (j = 10) : (j = 15);
    // printf("%d\n%d\n", i, j);


    int x = 10, y = 20;
    x == 20 && y != 10 ? printf("true\n"):printf("false\n");
    
    
    



























    //  int order, stock = 100;
    //  char credit;
    //  char y;
    //  char n;
    //  printf("enter the quantity ordered by customer: ");
    //  scanf("%d", &order);
    //  printf("is your credit OK, Press y or N: ");
    //  scanf("%c%c", &y, &n);
     
    //  if (order <= stock && credit == y)
    //  {
    //      printf("supply items to the consumer\n");
    //  }
    //  else if (order <= stock && credit != y)
    //  {
    //      printf("maintain your credit\n");
    //  }
    //  else if (order > stock && credit == y)
    //  {
    //      printf("we dont have enough item, we will ship it on 12/04/20\n");
    //  }
    //  else
    //  {
    //      printf("invalid input\n");
    //  }
     
     
     
     
     return 0;
 }
 