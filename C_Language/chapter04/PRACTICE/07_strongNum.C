#include<stdio.h>

int main(int argc, char const *argv[])
{
    int m, n, d, i, temp, rem;
    printf("enter first digit: ");
    scanf("%d", &m);
    printf("enter second digit: ");
    scanf("%d", &n);
    printf("enter the digit to count: ");
    scanf("%d", &d);
    for(i = m; i <= n; i++;)
    {
       
    }
    return 0;
}
