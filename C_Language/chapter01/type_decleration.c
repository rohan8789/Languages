#include <stdio.h>

int main()
{
    // int a=4 type decleration
    // int a = 4, b, c; //type deceleration
    // b = c = a;
    // printf("value of a is %d", a);
    // printf("value of b is %d", b);
    // printf("value of c is %d", c);
     
     float a= 7.3;
     float b= a+ 8.9;
     printf("value of b is %f", b);

     //what if we change the order, lets see
    //   float b= a+ 8.9;
    //  float a=7.3;
    //  printf("value of b is %f", b);

    //this will throw error
    return 0;

}
